[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect rule - routes all unmatched requests to index.html
# This allows React Router to handle client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
